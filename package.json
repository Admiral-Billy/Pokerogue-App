{
  "name": "Pokerogue",
  "version": "1.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package:win": "electron-packager . --overwrite --platform=win32 --arch=x64 --out=dist --icon=icons/PR --extra-resource=update-game.js --extra-resource=update-game.bat",
    "package:linux": "electron-packager . --overwrite --platform=linux --arch=x64 --out=dist --icon=icons/PR --extra-resource=update-game.js --extra-resource=update-game.sh",
    "package:mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --out=dist --icon=icons/PR --extra-resource=update-game.js --extra-resource=update-game.command"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^29.2.0",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "tree-kill": "^1.2.2"
  }
}